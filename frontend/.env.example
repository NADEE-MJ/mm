# API Configuration
# For development, point to your local backend
# For production, leave empty to use same-origin requests
VITE_API_URL=http://localhost:8155

# Note: TMDB and OMDB API keys are now configured on the backend
# See backend/.env.example for API key configuration
