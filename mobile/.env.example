# API Configuration
# Replace YOUR_LOCAL_IP with your computer's local IP address
# Find it with: ip addr show (Linux) or ifconfig (macOS)
# DO NOT use localhost - it won't work on mobile devices
# The port should match your backend (default: 8000 via main.py, or 8001 via config)
EXPO_PUBLIC_API_URL=http://YOUR_LOCAL_IP:8000/api
